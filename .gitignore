# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Per-workspace runtime state
**/.openclaw/

# Workspace config — contains secrets (bot tokens); never commit
workspace.config.json

# Portable runtime state
telegram-mcp-server/.env.local
telegram-mcp-server/queue.db

# Git worktrees (local-only, not for committing)
.worktrees/

# Scratch outputs
tmp/
dist/
coverage/
node_modules/
relay/

dev-server.log

workspace-identity.mdc
copilot-instructions.md
.claude/settings.local.json
.env
# Third-party skills
codebase-design/
logs/
.claude/.graph-lens-state.json
# memory-index CLI local index — regenerated via `npm run memory-index -- build`
.memory-index.json
